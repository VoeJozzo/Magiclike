# Frostbite Mage — alternate arts (saved for future reference)

The active card art is `../art.png`. The PNGs in this folder are **alternates only** —
they are NOT wired into the card and the engine ignores them (only `art.png` is loaded).
They are parked here as "commented out" options to swap in or revisit later.

To promote one: copy its 64x32 PNG over `../art.png`.

| file | provenance |
|---|---|
| `alt_01_pixellab.png` | The pixellab C2–C6-program portrait originally shipped in PR #137. Displaced 2026-07-02 when Joe picked the ChatGPT (GPT-5.5) render from PR #124 in a side-by-side; stashed here per the basic-land alts convention. Prompt record: the skill's `references/claude-prompts.txt`. |
