# Wiki moved

This documentation cluster was absorbed into the k-wiki vault on the Claudhaus (`C:\Users\Claude\k-wiki\magiclike\`) on 2026-08-05, retiring the repo-wiki junction pattern: all sessions (Claude's and Joe's reading) happen on that machine, so the vault is the single home. Pages there carry the vault's frontmatter schema (descriptions, scope, access instrumentation) and version with the vault's git.

The full history of these pages up to the move lives in this repo's history (last version: the parent of the commit that added this README, taken from `dev`).
