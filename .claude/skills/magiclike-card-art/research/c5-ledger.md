# C5 art-skill A/B — results ledger

Candidate **c5** = SKILL-control + the *"Diagnose-then-iterate"* block
(read the canvas honestly → name the worst concrete flaw → seed-lock/inpaint at it, never reroll blind).
Reviewer: the user, blind to arm identity. Reviewed via the applet from round 1 (no metric/elicitation drift).
Emoji-strip + pool-quality metric fixed across all 20 rounds. Verdicts: `verdicts_c5.json`.

## Headline (n=20, pool-quality vote)

- TREATMENT (c5) better: **7**
- CONTROL better: **10**
- tie: **3**

Decisive **7 T / 10 C** (n=17), two-sided binomial p = **0.629** vs 50/50 — **no detectable difference** (if anything, a slight, non-significant lean to control).
Keeper-frame provenance: 5 treatment / 11 control (n=16), p=0.21 — same direction, also non-significant.

## Verdict

C5 (Diagnose-then-iterate) does **not** beat control. Combined with C4's null, that's **two** prompt/skill-block interventions that fail to move the needle — evidence that the high-value lever is the human-in-the-loop review, not the agent's solo skill block.

## Per-card pool vote

- `archdemon_of_bargains` → TREATMENT (c5)
- `bleach` → CONTROL
- `bone_reliquary` → tie
- `curse_shade` → CONTROL
- `deepseam_quarry` → TREATMENT (c5)
- `elder_of_the_grove` → tie
- `flame_summoner` → TREATMENT (c5)
- `freeze_moment` → TREATMENT (c5)
- `giant_growth` → CONTROL
- `hill_giant` → CONTROL
- `living_lands` → CONTROL
- `petrify` → TREATMENT (c5)
- `sentinel_colossus` → CONTROL
- `sky_champion` → CONTROL
- `spectral_procession` → tie
- `symbiote_tree` → CONTROL
- `twin_strike` → TREATMENT (c5)
- `verdant_verge` → TREATMENT (c5)
- `vile_edict` → CONTROL
- `wildfire_devil` → CONTROL
