# Mana symbol design source

`manaiconsv13.jsx` — original React preview component the WUBRG mana symbols
were derived from. Reference only; not loaded by the prototype (no React
runtime). The compiled outputs ship at `assets/mana/{W,U,B,R,G}.svg`.

Authored by Claude in a separate session; see top-level `LICENSES.md`.
